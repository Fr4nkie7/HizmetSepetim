# 🚀 HizmetSepetim – v1.0.0  
### 📌 Tarih: 06.12.2025  
### 📌 Durum: İlk Kararlı Sürüm (Stable Release)

Bu doküman, HizmetSepetim uygulamasının ilk kararlı dağıtımı olan **v1.0.0** sürümünde gerçekleştirilen tüm teknik geliştirmeleri, mimari kararları, ekranları, API kullanımını, veri akışını ve sistem davranışlarını ayrıntılı olarak açıklar.

---

# 1️⃣ Genel Bakış

**HizmetSepetim v1.0.0**, uçtan uca çalışan tam entegre bir mobil hizmet satın alma sistemidir.  
Bu sürüm; istemci tarafı (Android), sunucu tarafı (Go API), veri tabanı (MySQL), bildirim altyapısı (Firebase), kullanıcı yönetimi ve sipariş sistemini kapsayan eksiksiz bir yapının ilk kararlı versiyonudur.

Bu sürümle birlikte:

- Mobil uygulama **tamamen kullanılabilir** hale geldi  
- API uçları stabil hale getirildi  
- Veritabanı şeması finalize edildi  
- Kullanıcı akışları baştan sona çalışıyor (Login → Hizmet → Adres → Ödeme → Randevu)  
- Bildirim sistemi entegre edildi  

---

# 2️⃣ Uygulama Mimarisi

### ✔ **Client: Android (Jetpack Compose)**  
- Compose Navigation  
- MVVM mimarisi  
- Retrofit2 + OkHttp  
- DataStore Preferences  
- Coil görüntü yükleme  
- Firebase Cloud Messaging  
- WorkManager  
- Accompanist System UI Controller

### ✔ **Backend: Go API**  
- net/http  
- JSON encode/decode  
- MySQL driver  
- Token tabanlı kullanıcı doğrulama  
- CRUD işlemleri  
- Error handling & status codes  
- Basit rate-limit mantığı  
- Cross-table JOIN yapıları  

### ✔ **Veritabanı (MySQL)**  
Tablolar:
- `users`
- `addresses`
- `categories`
- `services`
- `addons`
- `orders`
- `support_tickets`

Tamamı v1.0.0’da aktif.

---

# 3️⃣ Kullanıcı Akışı (End-to-End Flow)

## 🟦 1) Login / Register
- API → `/login` & `/register`
- Giriş başarılı → JWT-like session token cihazda **DataStore** içinde saklanır
- Token tüm isteklerde **Authorization header** olarak gönderilir

## 🟦 2) Ana Sayfa & Kategori Servisleri
- Home ekranı, orchestrator ViewModel ile API'dan kategorileri ve hizmetleri çeker  
- Scroll + LazyColumn yapısı  
- Coil ile hızlı görsel yükleme  

## 🟦 3) Ürün / Hizmet Detay Sayfası
Gösterilen veriler:
- Hizmet adı  
- Açıklama  
- Fiyat  
- Ek hizmet (addon) listesi  
- Kullanıcı puanları (mock hazırlandı)  

Addon seçimi → Local state'de tutulur, ödeme ekranına aktarılır.

## 🟦 4) Adres Yönetimi
Tüm adres işlemleri v1.0.0’da tamamlandı:
- Adres listeleme  
- Adres ekleme  
- Adres düzenleme  
- Adres seçme (checkout flow)  

API uçları:
- `/get_addresses`
- `/add_address`
- `/update_address`

Validations:
- Boş alan kontrolleri  
- Hatalı telefon formatı önleme  
- Eksik semt/ilçe hatası  

## 🟦 5) Ödeme & Randevu Ekranı
Ödeme sayfası tamamen **frontend simülasyonudur** (gerçek banka entegrasyonu yok → MVP için doğrudur).


Randevu verisi:
- Tarih → DatePicker
- Saat → Bottom Sheet (09:00 – 20:30 arası otomatik slot üretimi)


API uçları:
- `/create_order`  
Sipariş tamamlanınca → “Satın alma başarılı”.

## 🟦 6) Profil Yönetimi
- Kullanıcı bilgilerini gösterme  
- Profil düzenleme (isim, telefon)  
- Logout → Token silinir  

---

# 4️⃣ Arka Plan İşlemleri

## 🔔 Firebase Bildirim Entegrasyonu
- Firebase Messaging token alımı  
- Token sunucuya iletilmesi  
- Arka planda mesaj dinleme  
- Bildirim çalışma testi yapıldı  
- Worker sınıfı oluşturuldu (manifest kaydı v1.0.0’da kaldırıldı)

## 📦 DataStore
Saklanan veri:
- user_id
- session_token
- username
- phone


Key-value hızlı erişim için optimize edildi.

---

# 5️⃣ API Uçları (Bu Sürümde Aktif Olanlar)

- `POST /register`
- `POST /login`
- `GET /get_categories`
- `GET /get_services?category_id=`
- `GET /get_addons?service_id=`
- `POST /add_address`
- `POST /update_address`
- `GET /get_addresses`
- `POST /create_order`
- `POST /support/start`
- `POST /support/send_message`

Tüm uçlarda:
- JSON input/output  
- Status code yönetimi  
- Error handling  
uygulandı.

---

# 6️⃣ Performans ve Optimizasyon

### ✔ LazyColumn optimizasyonu  
### ✔ Gereksiz recomposition azaltıldı  
### ✔ Coil image caching aktif  
### ✔ Retrofit timeout konfigürasyonu eklendi  
### ✔ Gönderilen API istekleri loglandı (debug için)  
### ✔ Compose tarafında "remember" kullanımı düzenlendi  
### ✔ BottomSheet state'leri tek noktadan yönetildi  

---

# 7️⃣ Güvenlik

- Debug modda API logları açık, release modda kapalı  
- Token zorunluluğu  
- SQL injection koruması (Go tarafında prepared statements)  
- Error mesajlarında hassas veri döndürülmüyor  
- İmzalı APK (keystore ile) oluşturuldu  
- Key güvenli şekilde saklanıyor  

---

# 8️⃣ Bilinen Eksikler (v1.0.1 için planlananlar)

- Adres düzenleme ekranı UI iyileştirmesi  
- Ödeme ekranı için validation geliştirmesi  
- Bildirim mesaj içeriklerinin çeşitlendirilmesi  
- Order geçmişi ekranı (henüz eklenmedi)  
- UI transition animasyonları eklenecek  

---

# 9️⃣ Screenshots
Tüm ekran görüntüleri için:  
👉 **`screenshots/version-history/v1.0.0/`**

---

# 🔟 Sonuç

Bu sürüm, HizmetSepetim'in:

- ilk çalışan prototipi,  
- ilk MVP’si,  
- ilk stabil sürümü,  
- ilk imzalı üretim APK’sı  

olarak tarihe geçmiştir.

Uygulama artık uçtan uca kullanıcı akışını sorunsuz tamamlayabilmekte, backend ile tam entegre çalışmakta ve bir hizmet satın alma platformunun temel tüm özelliklerini sağlamaktadır.

---

Hazırlayan: **Buğra Akdemir**  
Tarih: **06.12.2025**
